// 1.cpp : Este archivo contiene la función "main". La ejecución del programa comienza y termina ahí.
//
#include <iostream>
void ej1()
{
    int num {13, 22. 5, 8, 34, 55, 60}
    int pares = 0;
    for (int i = 0; i < num[i], i++)
    {
        if (num[i] % 2 = 0)
        {
            pares = pares + 1;
        }
    }
    cout << "el numero de pares es" << pares << endl;
}

void ej2()
{
    int cont = 0;
    int total=0;
    cout << "cuantos numeros son?" << endl;
    cin >> cant;
    for (int i = 0; i < cant; i++)
    {
        cout << "ingrese el numero " << i << endl;
        cin >> cont;
        total = total + cont;
    }
    cout << "la suma de los numeros es" << total << endl;
}

void ej3()
{
    int mat = { {3,2,1}, {3,2,1}, {3,2,1} }
    for (int i = 0; i < 3; i++)
    {
        for (int j = 0; j < 3; j++)
        {

        }
    }
}

void ej4(int num; int vec[], bool flag)
{
    for (int i = 0; i < vec[i]; i++)
    {
      
    }
}

int main()
{
    std::cout << "Hello World!\n";
    cout << "hola" << endl;
}

// Ejecutar programa: Ctrl + F5 o menú Depurar > Iniciar sin depurar
// Depurar programa: F5 o menú Depurar > Iniciar depuración

// Sugerencias para primeros pasos: 1. Use la ventana del Explorador de soluciones para agregar y administrar archivos
//   2. Use la ventana de Team Explorer para conectar con el control de código fuente
//   3. Use la ventana de salida para ver la salida de compilación y otros mensajes
//   4. Use la ventana Lista de errores para ver los errores
//   5. Vaya a Proyecto > Agregar nuevo elemento para crear nuevos archivos de código, o a Proyecto > Agregar elemento existente para agregar archivos de código existentes al proyecto
//   6. En el futuro, para volver a abrir este proyecto, vaya a Archivo > Abrir > Proyecto y seleccione el archivo .sln
